# CONTRIBUTING · 共建指南

三种贡献方式：**补语料**（缺卷）｜**补案例卡**｜**纠错**（引文 / 事实）。

---

## 一、补卷手册（语料缺口：卷 189、214-218、223）

1. **取得文本**：需要与现有格式兼容的全文——卷首形如 `资治通鉴第X卷（回目录）`，卷内为「原文段 + 白话段」交替
2. **并入**：转 UTF-8 后，将缺卷内容追加到 `skill/data/zztj.txt` 对应位置（卷58-294 在前、卷1-57 在后）
3. **重建索引**：重新生成 `skill/data/zztj_index.json`（卷 → 行号 / 纪次 / 年份）
4. **校验**：`python skill/tools/retrieve.py 安禄山 --src zztj --vol 217` 能命中即并入成功
5. **修订引文**：语料行号变化后，核对并更新 `skill/data/quotes.json` 中受影响引文的 `line` 字段
6. **提交**：PR 中注明数据来源与版本

> ⚠️ 版权注意：白话译文版权归出版方，**不要**将语料全文提交到公开仓库；公版文言底本除外。

## 二、补充案例卡

1. 复制 `skill/cases/_TEMPLATE.md`，追加到对应主题文件（F01–F12）
2. **九字段必填**：出处 / 情境 / 当时的选项 / 判断与推理 / 结果 / 显性代价 / 隐性代价 / 规律启发 / 风险提醒 / 原文
3. **引文核验**（硬性）：原文必须能在 `skill/data/zztj.txt` 或 `shiji.txt` 中检索到（给出卷次/篇名），并在 `skill/data/quotes.json` 登记（label → 出处 + 行号 + 引文）
4. **更新索引**：`skill/cases/_index.json` 增加条目（id / 标题 / 文件 / 来源 / cite / kernels）
5. **重建站点数据**：`python tools/build_site_data.py`
6. **写作红线**（与 SKILL.md 质量闸门一致）：
   - 显性代价（钱 / 命 / 位置）与隐性代价（名声 / 结构 / 后代）**分开列**
   - "判断对错"与"结果好坏"**分开评价**（判断对的可能输——幸存者偏差）
   - 风险提醒**必含失效条件**；禁止"历史证明……"式宿命断言
   - 无出处的"史实"一律不写

## 三、纠错

开 Issue，附三要素：**错误位置 / 应有内容 / 依据（卷次或篇名）**。

## 四、开发

| 模块 | 位置 | 说明 |
|------|------|------|
| 站点 | `docs/` | 纯静态、零依赖、无构建；本地预览 `python -m http.server` 后访问 `/docs/index.html` |
| 数据管线 | `tools/build_site_data.py` | 案例卡 → 站点数据（`docs/assets/data.js`） |
| 检索工具 | `skill/tools/retrieve.py` | 关键词 / 内核 / 卷次 检索 |

Commit 规范：一句话说清"改了什么、为什么"；涉及引文的改动必须给出核验方式（卷次 + 检索命令）。

